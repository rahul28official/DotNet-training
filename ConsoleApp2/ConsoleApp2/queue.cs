using System;
using System.Collections;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace ConsoleApp2
{
    internal class queue
    {
    }
    /*class Program
    {
        static void Main()
        {
            Queue q=new Queue();
            q.Enqueue(3);
            q.Enqueue(4);
            q.Enqueue("rahul");

            Console.WriteLine("the number of items in queue :"+q.Count);
            while(q.Count > 0)
            {
                Console.WriteLine(q.Dequeue());
            }
            Console.WriteLine("the number of items in queue :" + q.Count);
        }
    }*/
}

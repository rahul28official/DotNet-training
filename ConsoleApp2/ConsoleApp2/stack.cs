using System;
using System.Collections;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace ConsoleApp2
{
    internal class stack
    {

    }
   /* class Program
    {
        static void Main()
        {
            Stack s=new Stack();
            s.Push(1);
            s.Push(2);
            s.Push(3);
            s.Push(4);
            Console.WriteLine("the number of element in stack :" + s.Count);

            while(s.Count>0)
            {
                Console.WriteLine(s.Pop());
            }
            Console.WriteLine("the number of element in stack :" + s.Count);
        }
    }*/
}
